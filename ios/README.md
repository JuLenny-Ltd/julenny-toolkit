# iOS app (deferred)

This directory is reserved for the iOS app that wraps the `core`
library via Objective-C++ bridging.

The app will be a native Swift app; the `core` C++ library is
statically linked into an `.xcframework` and called from Swift via
an Obj-C++ bridge layer.

For now: empty.
