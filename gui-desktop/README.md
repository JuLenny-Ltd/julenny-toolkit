# Desktop GUI (deferred)

This directory is reserved for the cross-platform desktop GUI shell
(Linux, Windows, macOS) that wraps the `core` library.

GUI framework choice (Qt vs alternatives) is deferred until we're
ready to start the desktop GUI work. See section 8 of
`plans/fhe-multi-party-architecture.md` in the platform repo.

For now: empty. The CLI in `../cli/` covers all v1 functionality.
